# Mission 01 — Website Rebuild

**Objective:** Rebuild `<site / page>` so that `<what must be true after>` (e.g.
loads under 1s, mobile-clean, on `<new stack>`) without losing `<what must not
break: SEO, forms, analytics>`.

**Situation**
- Current stack / host: `<...>`
- Live URL(s): `<...>`
- Source location: `<repo path or "none, scrape live">`
- Traffic that must not drop: `<top pages / keywords>`

**Constraints**
- Stack allowed: `<...>` · Budget: `<...>` · Deadline: `<...>`
- Must preserve: `<URLs / redirects / meta / pixels>`

**Definition of Victory**
- `<measurable: Lighthouse ≥ 90, all forms submit, 301s in place>`

**Known Unknowns (recon candidates)**
- Are current URLs indexed and ranking? `<check>`
- Any server-side logic / forms behind the pages? `<check>`
- CMS or hand-built? `<check>`

**Out of Scope:** `<redesign of brand, new copy, ...>`

---
**Order:** `Read SUCCESS.md and this mission. Wargame it to the 8-Point Standard. Write the plan to wargames/01-website-rebuild.md.`
